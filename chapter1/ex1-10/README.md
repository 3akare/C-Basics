# Exercise 1-10

Replace each tab by \t, each backspace by \b, and each backslash by \\. This makes tabs and backspaces visible in an unambiguous way.

## Solution
The solution replaces each tab by \t, each backspace by \b, and each backslash by \\. This makes tabs and backspaces visible in an unambiguous way.

## How to Run
```bash
gcc solution.c -o solution
./solution
```