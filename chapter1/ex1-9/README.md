# Exercise 1-9

Copies input to output, replacing sequences of one or more blanks with a single blank.

## Solution
The solution copies input to output, replacing sequences of one or more blanks with a single blank.

## How to Run
```bash
gcc solution.c -o solution
./solution
```